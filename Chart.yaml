apiVersion: v2
name: api-app
description: A Helm chart for api-app
type: application
version: 0.1.0
appVersion: "1.16.0"

dependencies:
- name: nginx-ingress
  version: "0.15.1"
  repository: "https://helm.nginx.com/stable"
